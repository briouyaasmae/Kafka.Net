to start kafka we should start zookeeper 
to start zookeeper we navigate to C:\zookeeper-3.4.10\bin and we type command "zkserver"
after that we navigate to kafka and we type command ".\bin\windows\kafka-server-start.bat ./config/server.properties"
